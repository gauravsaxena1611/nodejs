var msg = require('./2.7-ExportAnonymousFunctionModule.js');

msg('Hello World');

/** The msg variable becomes function expression in the above example. 
 * So, you can invoke the function using parenthesis (). 
 * 
 * Run the above example and see the output as shown below. */
