exports.myDateTime = function () {
  return Date();
};

// This example creates a module that returns a date and time object
// We use the exports keyword to make properties and methods available outside the module file.